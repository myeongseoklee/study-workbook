node_modules/
.env
dist/
*.log
.DS_Store
rag-index.json
.turbo/

# 검증관이 오답을 주입할 때 쓰는 임시 구현 디렉토리 (STUDY_TARGET=_probe)
packages/*/_probe/

# 학습 기록 worktree (study-log 브랜치가 상주한다)
.study-log/

# 풀이 worktree (sol/{패키지}/{과제번호} 브랜치를 과제마다 하나씩 연다)
.sol/
