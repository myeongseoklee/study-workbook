# study

학습 주제별 워크북 모노레포. 각 패키지가 지식 문서 · 문제 · 정답 테스트를 함께 담는다.

**규약의 단일 진실 원천은 [`README.md`](README.md)다.** 문서화 / 문제·정답 분리 / 기술 스택 / 풀이 브랜치 / 패키지 추가 — 5개 규약이 거기 있다. 하네스와 에이전트는 그 규약을 집행하며, 규약 내용을 중복 서술하지 않는다. 어긋나면 README가 맞다.

## 하네스: 레포 운영

**목표:** 패키지·과제 추가, 풀이 지원, 규약 감사를 규약에 맞게 수행하고, 규약 위반이 조용히 쌓이지 않게 한다.

**트리거:** 이 레포의 운영 작업 요청 시 `study-repo` 스킬을 사용하라 — 패키지 추가, 과제 추가·검증, 풀이 시작·판정, 규약 감사, 브랜치 정리. 단순 질문은 직접 응답 가능.

**경계:** 학습 자료를 **새로 생성**하는 것은 이 하네스가 아니라 `cc-system`의 `study-material-generator` 담당이다. 이 하네스는 **생성된 레포를 운영**한다. 자료 생성 규격(문항 작성법·분량·암기 선별)은 그 스킬이 정하고, 배치·명명·스택·브랜치는 이 레포 README가 정한다.

**변경 이력:**

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-05 | 초기 구성 | 전체 | 모노레포 전환 후 규약을 사람이 기억해서 지키는 상태였다. 규약 위반은 **조용히** 쌓인다 — 워크북 정답이 노출되거나, `package.json` 스크립트가 빠지거나, 풀이가 main에 머지돼도 아무 신호가 없다. 그래서 (1) 셀 수 있는 규약은 `audit-conventions.js`로 자동 검출하고, (2) 스캐폴딩을 스크립트로 고정해 설정 누락을 원천 차단하고, (3) 과제 저작을 **생성-검증 팀**으로 분리했다. ③이 핵심 — 통과하는 테스트만 보면 *아무것도 검사하지 않는 테스트*가 그대로 남으므로, `verify-assignment.sh`가 스켈레톤 상태에서 실패하는지를 먼저 확인하고 `assignment-verifier`가 흔한 오답을 겨냥하는지까지 검증한다. 구축 시 실측으로 기존 drift 3건(브랜치 명명 2, 암기 항목 형식 1)을 검출했고, 빈 테스트 함정도 실제로 잡히는 것을 확인했다 |
