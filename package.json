{
  "name": "study",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "학습 주제별 워크북 모노레포 — 지식 문서 + 실습 과제 + 정답 테스트",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "packages": "node -e \"const fs=require('fs');for(const d of fs.readdirSync('packages'))console.log(' -',d)\""
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.5",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
