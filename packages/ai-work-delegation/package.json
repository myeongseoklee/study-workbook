{
  "name": "ai-work-delegation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI에게 일을 시키는 방식의 재설계 — 답이 아니라 후보를 생성시킨다. 연역·귀납·귀추의 분업, 전제 부정(케이블 끊기), 역할 분리, 생산성 지표의 재정의",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@study/testkit": "workspace:*"
  }
}
