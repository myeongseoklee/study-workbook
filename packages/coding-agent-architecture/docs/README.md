# 코딩 에이전트 만들기 — 학습 자료 (전체 지도)

뉴런데브 채널 재생목록 [코딩에이전트 만들기](https://www.youtube.com/playlist?list=PLGjmLnACrYZI)를 학습용으로 재구성한 자료다. 재생목록은 계속 추가되며, 이 자료는 **회차별 폴더 + 회차를 넘는 누적 공통 문서**의 하이브리드 구조로 성장한다.

## 구조

```
packages/coding-agent-architecture/
├── docs/
│   ├── README.md              ← 지금 이 파일 (전체 지도)
│   ├── ep01-concepts/         ← 강의 #1 (개념편)
│   │   └── 00-overview.md ... 06-local-llm.md
│   ├── ep02-business-agent/   ← 강의 #2 (기업형 에이전트)
│   │   ├── 00-overview.md ... 10-tech-stack.md
│   │   └── assets/frames/     ← 강의 슬라이드 캡처
│   ├── (ep03-.../ ...)        ← 새 영상이 오면 여기에 추가
│   ├── 90-must-memorize.md    ← 암기 카드 (전 회차 누적)
│   ├── 91-glossary.md         ← 용어집 (전 회차 누적)
│   └── 99-references.md       ← 외부 공식 문서 (전 회차 누적)
├── workbook/
│   ├── 92-workbook.md         ← 문제 (1·2강 누적, 정답 0건)
│   └── 93-solutions.md        ← 정답·해설
├── src/                       🎯 코딩 문제 (TODO 스켈레톤)
└── solutions/                 ✅ 정답 = 테스트 코드
```

> 문제와 정답이 파일로 갈라져 있다. 접기로 감추면 렌더러·검색·AI 요약으로 계속 새어 나가므로 분리가 원칙이다 (레포 [`README.md`](../../../README.md) § 규약 2).

## 어떻게 공부하나

1. **이 README로 지도 한 번 훑기** — 지금 어디까지 나왔고 뭘 배우는지.
2. **회차 폴더를 번호 순서대로 정독** (`ep01-concepts/00 → 06`). 각 파일 뼈대: `학습 목표 → 핵심 원리(왜) → 필수 지식(어떻게) → 우리 프로젝트와의 연결`.
3. **한 챕터 끝날 때마다 `90-must-memorize.md`에서 해당 항목 체크.**
4. **모르는 용어는 `91-glossary.md`를 사전처럼** 옆에 열어두고 그때그때 찾기.
5. **새 회차가 나오면 그 폴더를 같은 방식으로 이어서.** 아래 개념 색인이 "이 개념은 몇 화에 있는지" 짚어준다.
6. **[`workbook/92-workbook.md`](../workbook/92-workbook.md)를 자료를 덮고 풀기.** 읽어서 아는 것과 꺼내 쓸 수 있는 것은 다르다. 정답은 [`93-solutions.md`](../workbook/93-solutions.md).
7. **코딩 과제는 풀이 브랜치에서** `src/`의 `🎯 TODO`를 채우고 `npm run test:{번호}`로 판정한다.
7. **실전·복습 직전엔 `must-memorize.md`만** 다시 훑기.

## 에피소드 목록

| 회차 | 폴더 | 성격 | 원본 영상 |
|---|---|---|---|
| #1 | `ep01-concepts/` | 개념편 (구현 없이 구성 요소 개관) | 코딩에이전트 만들기 #1 (약 1h54m) + #1-사고로 꺼짐(도입부, 약 23m) |
| #2 | `ep02-business-agent/` | 세션·컨텍스트의 실체 + 기업 납품 요구·개발 계획 | 코딩에이전트 만들기 #2 비지니스용 에이전트 (라이브, 약 1h23m) |

## 개념 → 회차 색인

같은 개념이 여러 회차에 걸칠 때, 어디서 처음 다루고 어디서 심화되는지 추적한다. (회차가 늘면 "심화" 열이 채워진다.)

| 개념 | 처음 다룬 곳 | 심화 |
|---|---|---|
| 왜 에이전트인가 / 워크플로우 vs 에이전트 | `ep01/01-why-agents` | — |
| 세션·턴·이터레이션 | `ep01/02-agent-loop` | — |
| 서브에이전트·골·에이전트 메시징 | `ep01/02-agent-loop` | — |
| 도구 = 제어권 / 전용 vs 범용 / 도구 vs 스킬 | `ep01/03-tools-as-control` | — |
| 도구 부수효과·리마인더·프로세스 실행 | `ep01/03-tools-as-control` | — |
| 훅 | `ep01/04-hooks` | — |
| 컨텍스트 희석 / 4시-7시 / 폴더 격리 | `ep01/05-context-and-cost` | — |
| 온프레미스 로컬 LLM (200B·Q5 하한, KV 캐시) | `ep01/06-local-llm` | `ep02/01-session-identity` (KV 캐시가 세션의 실체) |
| 세션의 정체 = KV 캐시 식별 ID / 프리픽스 캐시 / 영속화·복원 | `ep02/01-session-identity` | — |
| 세션 서버 아키텍처 (인스턴스 vs 스트림, 이벤트 소싱) | `ep02/02-session-server` | — |
| 기반 컨텍스트 조립 (모델별 프롬프트·도구·스킬 계산·이름 우선순위) | `ep02/03-context-assembly` | — |
| 컴팩션 (트리거 판정·4방식·잘림 정책·컴팩트 레코드) | `ep01/05-context-and-cost` (4시-7시 경험칙) | `ep02/04-compaction` (구현 관점 전면) |
| 팀 운영 모델 전환 / 기술셋 교체 | `ep02/05-team-operating-model` | — |
| 온프레미스가 만드는 시장 / 고객군 | `ep01/06-local-llm` (하드웨어 산수) | `ep02/06-why-onprem` (사업 근거) |
| 비즈니스 요구 5종 (조직·정보시스템·업무겸용·모델라우터·가드레일) | `ep02/07-business-requirements` | — |
| 배포 형태 (설치환경·손코딩 관여도·과금) | `ep02/08-deployment-modes` | — |
| 비즈니스 서버 개발항목 (인증인가·공유드라이브·모델라우터·검색센터) | `ep02/09-business-server` | — |
| 기술 스택 (터보 모노레포·PGVector·단일 Express) | `ep02/10-tech-stack` | — |

## 변경 이력

| 날짜 | 회차 | 변경 내용 |
|---|---|---|
| 2026-07-29 | #1 | 최초 생성 (개념편 7파일 + 누적 공통 문서 3종). 하이브리드 구조로 재편(회차 폴더 + 최상위 누적 문서 + 이 README). |
| 2026-08-05 | #2 | `study` 모노레포 `packages/coding-agent-architecture/`로 이전(문제·정답 분리 규약 적용). `ep02-business-agent/` 추가 (본문 11파일 + 워크북 + 슬라이드 캡처 6장). 누적 문서 갱신 — `must-memorize.md`에 #2 항목 25개(신규 섹션 2개: 구현 규칙 / 비즈니스 제품 요구), `glossary.md`에 레이어 6~7 신설(용어 30여 종), `references.md`에 #2 원본·Turborepo·pgvector·PowerShell 인코딩·RHEL 컨테이너 항목과 수치 검증 안내 추가. 자막이 없는 영상이라 로컬 STT로 전사한 뒤 슬라이드 캡처로 교차 확인했고, 자체 시스템 시연 구간은 제외했다. |
