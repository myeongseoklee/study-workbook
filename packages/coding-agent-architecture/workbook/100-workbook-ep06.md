# 워크북 (6강) — 빠르게 보이는 것과 실제로 유지되는 것

## 1. 회수 연습

1. 워커를 이벤트 단위로 짧게 만드는 이유와, 모델 추론만 예외인 이유를 설명하라.
2. 프리픽스 캐시가 성립할 때와 깨졌을 때 로그의 처리 비율은 어떻게 달라지는가?
3. 프리필과 디코드에서 전송량·병목이 어떻게 다른가?
4. “개발 4 : 리팩토링 6”이라는 비율이 의미하는 운영 원칙을 설명하라.

## 2. 판단 문제

5. 작은 컨텍스트에서 TPS가 높지만 실제 200K 토큰 세션에서 처리 비율이 천천히 오른다. 무엇을 의심하고 어떤 로그를 확인할 것인가?
6. `Reactor`, `TurnServer`, `Worker`가 모두 같은 역할의 일부를 가리킨다. 이 상태가 왜 기능 문제가 아니라 구조 문제의 신호인가?
7. 팀이 “기능을 먼저 많이 만들고 나중에 리팩토링하자”고 한다. 이 회차의 근거로 반박하라.

## 3. 재현 과제

8. 다음 이벤트 목록에서 `sessionId`별로 순서를 복원하고, 중복 시퀀스는 한 번만 반영하는 함수를 구현하라. 입력 이벤트는 순서가 뒤섞이고 중복될 수 있다.

```ts
type Event = { sessionId: string; sequence: number; type: string; payload: unknown };
function replay(events: Event[]): Event[] { /* TODO */ }
```

성공 기준: 세션별 오름차순, 동일 세션·시퀀스 중복 제거, 서로 다른 세션은 입력에 처음 등장한 순서 유지.

9. 캐시 로그의 `processing` 값 `[68, 91, 100]`과 `[12, 34, 56]`을 각각 판정하라. 전자는 무엇이 성립한 것이고 후자는 무엇이 깨진 것인가?

## 4. 오답 노트

- 내가 TPS를 컨텍스트 크기와 분리해 판단한 순간:
- 내가 이름 불일치를 단순 취향 문제로 넘긴 순간:
- 내가 생성 속도만 보고 유지보수 비용을 누락한 순간:
