# 정답과 해설 — 코딩 에이전트 만들기 (1·2·3강)

> 먼저 [92-workbook.md](92-workbook.md)를 풀고 나서 열어보라. 문항 번호가 그대로 대응한다.
>
> 코딩 과제(3-1 ~ 3-9)의 정답은 이 파일에 없다. 판정은 `tests/`의 명세가 하고 참고 구현은 `solutions/`에 있다 — 자기 구현을 `pnpm test {번호}`로 돌려 판정한다. 이 파일에는 **문서 과제(3-10)의 판정 체크리스트**만 있다.

## 파트 1. 회수 연습

### 1-A-1

**제어 흐름을 누가 잡는가.** 코드가 잡으면 워크플로우, 모델이 잡으면 에이전트다.

다른 기준(도구를 쓰는지, LLM을 몇 번 호출하는지, 프롬프트가 긴지)이 부차적인 이유: 그것들은 모두 워크플로우로도 구현할 수 있다. 반복 횟수와 방법을 **모델이 자율 결정**하는 순간에만 에이전트가 된다.

📍 되짚기: `../docs/ep01-concepts/01-why-agents.md` § 필수 지식 — 워크플로우 vs 에이전트

### 1-A-2

다섯 요소가 모두 "모델에게 제어권을 얼마나 넘기는가"의 문제로 수렴한다.

- **세션** — 모델과 함께 써 내려가는 컨텍스트. 넘긴 제어권이 누적되는 자리
- **서브에이전트** — 세션 생성 권한을 모델에게 넘긴 것
- **골** — 목표 판단을 모델에게 넘긴 것
- **도구** — 외부 행동 권한을 넘긴 것 (코드에 가두면 워크플로우)
- **훅** — 반대 방향. 넘긴 제어권 일부를 코드가 되찾는 장치

📍 되짚기: `../docs/ep01-concepts/00-overview.md` § 큰 그림 / `02-agent-loop.md` § 정리

### 1-A-3

**서브에이전트의 실체는 세션이다.** 모델이 도구를 통해 스스로 생성한 세션.

차이는 **누가 열었는가** 하나뿐이다 — 사람이 열면 세션, 모델이 열면 서브에이전트. "에이전트"라는 이름은 마케팅 용어이고, 메커니즘은 "새 세션을 열어 끝난 뒤 최종 응답을 돌려주는 도구"다. 여는 진짜 이유는 **컨텍스트 분리**다.

📍 되짚기: `../docs/ep01-concepts/02-agent-loop.md` § 서브에이전트 = 모델이 만든 세션

### 1-A-4

**세션 ⊃ 턴 ⊃ 이터레이션** (바깥이 안을 담는 그릇).

- **세션** — 턴들을 담는 그릇. 컨텍스트를 히스토리에 따라 단조 증가시킨다
- **턴** — 요청 트리거로부터 최종 응답 출력까지
- **이터레이션** — 한 턴 안에서 일어나는 연속적인 모델 호출 (도구 호출 → 결과 → 또 호출)

📍 되짚기: `../docs/ep01-concepts/02-agent-loop.md` § 세 가지 기본 요소

### 1-B-1

**얻는 것**: 유연성·창의성. 새 상황에 스스로 방법을 찾는다.
**잃는 것**: 결정성. 예측 불가, 뽑기(같은 입력에 다른 결과), 토큰 폭탄.

📍 되짚기: `../docs/ep01-concepts/01-why-agents.md`

### 1-B-2

**전용 도구**는 인자 스키마가 좁아 약한 모델의 실수를 줄인다. **범용(쉘) 도구** 하나로 비비려면 강한 모델이 필요하다 — 명령 조합을 스스로 정확히 만들어야 하기 때문이다.

사례: **Claude Code = 전용 도구 다수**, **Codex = 쉘 통합**.

📍 되짚기: `../docs/ep01-concepts/03-tools-as-control.md` § 도구 설계의 세 축

### 1-B-3

**프로세스 실행** — 본체가 안전하다(도구가 죽어도 에이전트는 멀쩡). 대가는 직렬화·프로세스 간 통신 비용.
**인메모리 실행** — 빠르다. 대가는 도구가 블로킹되면 에이전트 전체가 늘어진다.

위험이 특히 커지는 조건: **단일 이벤트 루프**. 한 도구가 이벤트 루프를 잡으면 전부 멈춘다.

📍 되짚기: `../docs/ep01-concepts/03-tools-as-control.md` § 도구 설계의 세 축

### 1-B-4

코드에 가두면 **워크플로우**, 도구로 주면 **모델 제어**가 된다.

정체는 **제어권 배분**이다. 즉 도구 목록을 설계하는 일이 곧 "모델에게 무엇을 맡기고 무엇을 코드가 결정할지"를 정하는 일이다.

📍 되짚기: `../docs/ep01-concepts/03-tools-as-control.md` § 핵심 원리

### 1-B-5

컨텍스트가 클수록 **KV 캐시가 VRAM을 더 잡아먹는다**. 어텐션의 Key·Value를 세션별로 캐싱하기 때문이다. 온프레미스에서는 이것이 동시 세션 수를 직접 제한한다.

그래서 컨텍스트 축소는 **비용 정책**이다. 성능 최적화가 아니라 "몇 명을 태울지"의 문제다.

📍 되짚기: `../docs/ep01-concepts/05-context-and-cost.md` / `06-local-llm.md`

### 1-C-1

컨텍스트를 시계로 볼 때 **4시에 마지막 요청**을 날린다(성능 최고점). **7시가 마지노선**이고, **9시 이후와 컴팩션 후는 다 쓰레기**다.

민감하거나 어려운 작업 기준의 경험칙이다 — 단순 작업은 더 밀어붙일 수 있다.

📍 되짚기: `../docs/ep01-concepts/05-context-and-cost.md` § 4시-7시 규칙

### 1-C-2

바이브 코딩은 비결정적이다(뽑기). 폴더로 격리하면 **뽑기가 그 폴더 안에서만 일어난다** — 수정 하나가 전역 파일을 건드리는 사고를 구조적으로 막는다.

**파일이 하나여도 폴더로 나눈다.** 격리 없이는 하루 수십 번 수정되는 제품을 안 깨고 유지할 수 없다.

📍 되짚기: `../docs/ep01-concepts/05-context-and-cost.md` § 4시-7시는 곧 아키텍처 요구다

### 1-C-3

**약 200B 이상.** 30B·120B는 제품 수준 코딩이 불가능하다. MiniMax M2급(약 200~220B)이 진입점이다.

📍 되짚기: `../docs/ep01-concepts/06-local-llm.md` § 두 개의 마지노선

### 1-C-4

**Q5 이상.** Q4는 모델이 아무리 커도 소용없다 — 원본 분포에서 벗어나 사실상 다른 모델이 된다.

근거 지표는 **KL 발산**이다. 약 0.03 이내에 들어야 원본 성향이 유지되고, 그 조건을 만족하는 게 Q5부터다.

📍 되짚기: `../docs/ep01-concepts/06-local-llm.md` § 두 개의 마지노선

### 1-C-5

**카드당 141GB, HBM3e.**

📍 되짚기: `../docs/ep01-concepts/06-local-llm.md` / [공식](https://www.nvidia.com/en-us/data-center/h200/)

### 1-C-6

**세션 약 20개, 개발자 3명 한계.**

계산: 노드 약 500GB(H200 약 4장) − 모델 적재(460B Q5 약 300GB) = 남는 약 200GB. 풀 컨텍스트 세션 하나가 10GB를 넘으므로 200 / 10 ≈ 20개. 개발자 1인이 동시에 여는 세션을 감안하면 3명 수준이다.

> 이 산수를 코드로 옮기는 것이 과제 3-2다.

📍 되짚기: `../docs/ep01-concepts/06-local-llm.md` § KV 캐시 산수

### 1-C-7

**1M**: Opus 4.8 / Opus 4.7 / Sonnet 5 / Fable 5.
**200K**: Haiku 4.5.

컨텍스트 크기는 정책에 따라 바뀌므로 정확한 최신값은 공식 문서로 확인한다.

📍 되짚기: `../docs/90-must-memorize.md` § 하드웨어 숫자 / `../docs/99-references.md`

### 1-D-1

원인은 **컨텍스트 희석**이다. 내가 흘린 **모순된 지시들이 누적**되어 모델이 어느 것을 따를지 흔들리는 상태다.

원인이 **아닌 것**: 어텐션의 한계, 모델의 능력 부족. 모델을 바꿔도 같은 대화에서는 재발한다.

📍 되짚기: `../docs/ep01-concepts/05-context-and-cost.md` § 핵심 원리

### 1-D-2

원인: 로그가 길어져 **초반 지시를 까먹은** 것.
처방: **리마인더 훅**으로 현재 단계를 컨텍스트 근처에 재삽입한다.

📍 되짚기: `../docs/ep01-concepts/04-hooks.md` § 턴 루프의 훅 지점들

### 1-D-3

**스트림 가드 훅**으로 중간에 끊는다. 같은 말이 반복되는 패턴을 감지해 생성을 중단시킨다.

📍 되짚기: `../docs/ep01-concepts/04-hooks.md`

### 1-D-4

**양자화와 모델 크기.** 대개 Q5 이하(Q4·Q3)를 쓴 것이거나, 200B 미만 모델이다. 프롬프트나 도구를 손보기 전에 이 둘을 먼저 확인한다.

📍 되짚기: `../docs/ep01-concepts/06-local-llm.md` § 두 개의 마지노선

### 1-D-5

**폴더·모듈 격리 실패.** 뽑기(비결정성)가 격리 경계 없이 전역에 퍼진 것이다.

📍 되짚기: `../docs/ep01-concepts/05-context-and-cost.md` § 폴더 격리

### 1-E-1

세션의 정체는 **모델이 실제로 들고 있는 KV 캐시의 식별 ID**다. 캐시 ID가 달라지면 다른 세션이다.

세션 두 개를 병렬로 진행하려면 각 세션의 대화가 이어져야 하므로 **KV 캐시도 두 세트**가 필요하다. 그래서 병렬 세션 수 = 캐시 세트 수 = 메모리 사용량이다.

📍 되짚기: `../docs/ep02-business-agent/01-session-identity.md` § 핵심 원리 / 영상 [00:06:00]

### 1-E-2

① **모델의 KV 캐시** — 프리픽스 캐시 유지, 영속화·복원용 고유 키
② **컨텍스트 구조체** — 그 ID에 쌓인 컨텍스트에 접근하는 권한
③ **도구·메시지큐** — 도구 실행 결과나 큐 이벤트를 보고할 대상 ID

합칠 수 없는 이유: **캐시 / 대화 / 작업의 생명주기가 서로 다르다.** 캐시가 만료돼도 대화는 읽혀야 하고, 도구가 비동기로 늦게 끝나도 결과를 보낼 곳이 남아 있어야 한다.

📍 되짚기: `../docs/ep02-business-agent/01-session-identity.md` § 식별 ID는 세 층에서 / 영상 [00:06:00]

### 1-E-3

약 **20분**. 내려간 뒤 재입장하면 앞부분을 전부 다시 처리해야 해서 프롬프트 프로세싱이 오래 걸린다. 그래서 내릴 때 **디스크에 직렬화(영속화)** 해 두고 재입장 시 **KV 캐시로 복원**한다.

📍 되짚기: `../docs/ep02-business-agent/01-session-identity.md` § 영속화·복원 / 영상 [00:08:00]

### 1-E-4

앞까지의 KV 캐시를 재계산하지 않고 **이전 세션의 캐시를 복사**해 오기 때문이다.

📍 되짚기: `../docs/ep02-business-agent/01-session-identity.md` § 포크가 즉시 되는 이유 / 영상 [00:07:00]

### 1-F-1

**기반 컨텍스트** — 시스템 프롬프트·프로젝트 환경·AGENTS.md 체인·도구·스킬 리스트 등. 세션 시작 시 계산 후 **고정**.
**턴 로그** — 요청·응답·도구 호출 히스토리. 뒤로만 자라는 **단조 증가 불변 리스트**.

📍 되짚기: `../docs/ep02-business-agent/03-context-assembly.md` § 핵심 원리 / 영상 [00:15:30]

### 1-F-2

**5% 이내**. 코덱스는 기본 약 **4%**를 먹고 시작한다. 기반이 자리를 많이 차지하면 **주인공(실제 작업)이 자리를 못 잡기** 때문이다.

📍 되짚기: `../docs/ep02-business-agent/03-context-assembly.md` / 영상 [00:27:00]~[00:27:30]

### 1-F-3

① 스킬을 로딩하면 **그 스킬이 쓸 수 있는 도구로 한정**
② 에이전트를 로딩하면 **그 에이전트의 도구가 지정**
③ **퍼미션(인가)** 에 따라 가용 도구가 갈림

셋이 겹쳐 세션 조립 시점에 동적으로 계산된다.

> 이 계산을 코드로 옮기는 것이 과제 3-3이다.

📍 되짚기: `../docs/ep02-business-agent/03-context-assembly.md` § 도구 리스트 / 영상 [00:19:00]

### 1-F-4

**이름 해소(우선순위) 규칙**. 클로드 코드에는 정해진 순서가 있고, 직접 만들려면 정의해야 한다.

같은 충돌이 **MCP·툴·프롬프트·커맨드·플러그인·룰스** 전부에서 발생한다 — 즉 아키텍처 필수 구성요소다.

📍 되짚기: `../docs/ep02-business-agent/03-context-assembly.md` § 스킬 리스트 / 영상 [00:21:30]~[00:22:00]

### 1-F-5

**현재 작업 위치 기준 계층 병합**이다. 경로에 걸리는 파일들이 위로 거슬러 올라가며 전부 합산되고, 그 결과를 **200K 안으로 축소**하는 기능이 붙어 있다. 병합 + 예산 관리가 한 세트다.

📍 되짚기: `../docs/ep02-business-agent/03-context-assembly.md` § AGENTS.md / 영상 [00:22:30]~[00:23:30]

### 1-F-6

시스템 프롬프트가 모델별로 다르므로 **기반 컨텍스트가 바뀌어 사실상 재로딩**이 된다. 기반이 변하면 프리픽스 캐시가 전부 무효다.

📍 되짚기: `../docs/ep02-business-agent/03-context-assembly.md` / 영상 [00:15:30]~[00:16:00]

### 1-F-7

**80%**. 단 컨텍스트는 실제 메모리 사용량이 아니라 **모델에게 보내는 내용**이므로, 보낼 전체 메시지를 파싱해 **토큰 수를 세야** 한다.

> 이 판정을 코드로 옮기는 것이 과제 3-4다.

📍 되짚기: `../docs/ep02-business-agent/04-compaction.md` § 80%가 최적인데 / 영상 [00:29:30]~[00:30:30]

### 1-F-8

**롤링 윈도우**(앞부분을 계속 뭉갠다) / **중간 절단**(중간에서 끊는다) / **정지**(리미트에 닿으면 끊어버린다). 이건 에이전트 버그가 아니라 **추론 서버에 원래 있는 정책**이다.

세 정책 모두 코딩 작업에 치명적이다(초반 지시 소실, 맥락 분절, 미완 종료). 그래서 잘림에 도달하기 전에 컴팩트해야 한다.

📍 되짚기: `../docs/ep02-business-agent/04-compaction.md` § 잘림이 일어난다 / 영상 [00:29:00]~[00:29:30]

### 1-F-9

262K 컨텍스트에서 기반이 약 5%, 턴 로그가 약 95%다. 컴팩트하면 전체 10% 수준이 되므로 **95%를 5%로 압축**한 셈이다 → 중요 정보 대량 분실.

추가 위험: **262K를 요약하는 과정 자체의 할루시네이션.** 환각 섞인 요약으로 민감한 코딩을 이어가게 된다.

📍 되짚기: `../docs/ep02-business-agent/04-compaction.md` § 방식 2 / 영상 [00:31:00]~[00:32:00]

### 1-G-1

**세션별 인스턴스**(클로드 코드) / **스트림 서버**(코덱스). 기준은 **클라이언트가 세션을 점유하는가** — 인스턴스 방식에는 세션을 점유한 클라이언트라는 개념이 있고, 스트림 방식에는 없다.

📍 되짚기: `../docs/ep02-business-agent/02-session-server.md` § 핵심 원리 / 영상 [00:08:30]~[00:09:30]

### 1-G-2

근거: 프리픽스 캐시 때문에 컨텍스트가 이미 **append-only 불변 리스트**다. 즉 구조가 이벤트 소싱과 같으므로 세션을 방으로 격리할 이유가 없다.

실익: **다중 클라이언트.** 모바일·브라우저·CLI가 같은 세션을 동시에 보고 던질 수 있다. 세션과 클라이언트가 연결을 맺지 않기 때문이다. 그래서 원격 지원에서 코덱스가 앞선다.

📍 되짚기: `../docs/ep02-business-agent/02-session-server.md` / 영상 [00:10:00]~[00:12:00]

### 1-G-3

단점은 **읽기가 느리다**는 것(상태를 알려면 사건을 재생해야 함). 그래서 보통 스냅샷(머티리얼라이즈드 뷰)을 만든다.

여기서는 **모델 추론이 압도적으로 느려서** 재생 비용이 티도 안 난다. 교훈: 아키텍처는 일반론으로 정하지 말고 **병목의 위치로 판단**한다.

📍 되짚기: `../docs/ep02-business-agent/02-session-server.md` § 단점이 무해한 이유 / 영상 [00:13:00]~[00:13:30]

### 1-G-4

**콜드 스트림**(세션 서버를 처음 열 때 과거 이벤트를 한 번에 가져옴) → **핫 스트림**(이후 실시간 이벤트 수신).

📍 되짚기: `../docs/ep02-business-agent/02-session-server.md` § 클라이언트 쪽에서 본 그림 / 영상 [00:14:00]

### 1-G-5

컴팩트 레코드가 **스냅샷 구분점**이 되어, 그 시점부터 **아래로만** 모델에 보낸다. 컴팩트는 여러 번 일어나므로 규칙은 **"마지막 컴팩트 레코드 아래로만 구성"** 이다. 전체 로그에는 컴팩트 레코드가 여러 개 존재한다(로그 자체는 보존).

> 이 재생기를 만드는 것이 과제 3-5다.

📍 되짚기: `../docs/ep02-business-agent/04-compaction.md` § 컴팩트 레코드와 불변 리스트 / 영상 [00:33:00]~[00:33:30]

### 1-G-6

**비즈니스(통합 대시보드) 서버를 먼저** 만든다 — 정책과 기능을 확정한 뒤 에이전트가 그것을 받아 동작한다. 이 서버는 AI가 관여하지 않는 평범한 웹 서버다.

순서를 뒤집으면: 권한·모델 할당·저장 위치·가드레일이 나중에 들어오면서 **에이전트의 컨텍스트 조립·도구 목록 계산·세션 발급 경로를 전부 다시** 손대게 된다(도구·스킬 리스트가 인가의 계산 결과이기 때문).

📍 되짚기: `../docs/ep02-business-agent/09-business-server.md` § 핵심 원리 / 영상 [01:06:00]

### 1-G-7

`apps` = **오케스트레이션**(빌드 단위 서비스), `packages` = **핵심 도메인**(라이브러리). 참조는 **`apps` → `packages` 단방향**.

`packages`에는 **react·express 같은 프레임워크 지식이 들어가면 안 된다** — 순수 도메인 로직과 모델만.

📍 되짚기: `../docs/ep02-business-agent/10-tech-stack.md` § 단방향 참조 규칙 / 영상 [01:19:00]~[01:19:30]

### 1-H-1

**온프레미스.** 깨지는 것 셋: **웹 검색 / 외부 모델 / 보안 프로그램과의 공존**(파수 같은 문서보안·파일 암호화 툴).

📍 되짚기: `../docs/ep02-business-agent/06-why-onprem.md` / 영상 [00:49:30]~[00:50:30]

### 1-H-2

기업들도 코딩 에이전트 자체는 큰 기술 격차를 만들지 않는다고 본다("그게 그거"). 사용자도 오픈코드에 API를 물려 쓴다.

상품성은 **조직 관리 / 사내 정보시스템(RAG·파인튜닝) / 일반 업무 겸용 / 모델 라우터 / 전사 가드레일** — 기존 제품이 지원하지 않는 영역이다.

📍 되짚기: `../docs/ep02-business-agent/06-why-onprem.md` § 기업이 사는 이유 / 영상 [00:50:30], [00:57:30]

### 1-H-3

핵심 요구: **엔터프라이즈 계정에 할당된 토큰이 소진되면 사내 LLM으로 무중단 전환.** (더해서 업무 패턴별 전환, 직접 선택도)

최소 구현: **개인이 사용할 모델이 무엇인지 서버가 내려주기.** 산업용은 회사가 LLM 서버를 소유하므로 회사가 모델을 지정한다.

📍 되짚기: `../docs/ep02-business-agent/07-business-requirements.md` § 4 / `09-business-server.md` § 3 / 영상 [00:55:30], [01:13:00]

### 1-H-4

**RHEL(레드햇) 엔터프라이즈 + 사내 보안 환경은 도커 컨테이너를 차단**한다(크롬 시크릿 탭 차단과 같은 류의 정책). 그래서 도커로 만들면 엔터프라이즈에서 안 되는 경우가 많아 **네이티브 설치형**으로 만들어야 한다.

📍 되짚기: `../docs/ep02-business-agent/08-deployment-modes.md` § 현장에서 뒤집히는 지점 / 영상 [01:01:30]~[01:02:30]

### 1-H-5

라이센스: 인터넷과 무관하므로 **타임 서버에 접근할 수 없다** → 컴퓨터 시간을 믿어야 한다 → 시간을 조작해 라이센스를 속이는 것에 대한 **방어**가 필요하다.

웹 검색: **벡터DB 인덱스로 대체**한다. 기업이 원하는 세트(뉴스·위키·특정 언론 크롤링 등)를 구성해 **"언제까지의 인덱스"** 로 납품하고, **갱신을 과금**한다(USB 방문 갱신). 깃허브도 카테고리별로 내려 인덱스화한다.

📍 되짚기: `../docs/ep02-business-agent/08-deployment-modes.md` § 축 3 / 영상 [01:04:00]~[01:05:30]

### 1-I-1

**SQLite.** 판단 기준은 데이터 양이 아니라 **배포 형태**다 — 온프레미스 관리 서버는 고객사마다 한 벌씩 들어가고 동시 사용자가 회사 인원 규모이며 설치가 쉬워야 한다. 이 조건에서 별도 DB 서버는 관리 대상만 하나 더 늘린다. 대규모 배포가 필요해지면 PostgreSQL로 간다.

파일시스템+JSON을 쓰지 않는 이유 둘: ① 폴더·파일이 금방 지저분해진다 ② **파일 액세스 동시성이 더 나쁘다** — 잠금을 직접 구현하게 되는데 그건 이미 DB가 하는 일이다.

📍 되짚기: `../docs/ep03-admin-implementation/01-storage-and-account.md` / 영상 [02:30]~[03:00]

### 1-I-2

**회사 이메일은 고유하지 않다.** 퇴사하면 주소가 없어지고 **같은 주소를 신입사원이 다시 받는다**(많은 회사가 그렇다).

무너지는 것: **감사 로그의 "누가"**다. 이메일을 영구 키로 쓰면 두 사람의 이력이 한 계정에 섞여, "누가 무엇을 했나"를 남기는 것이 목적인 시스템에서 그 목적 자체가 깨진다. 사번처럼 재사용되지 않는 키를 쓸 자리를 미리 알아 두는 것과, 이메일이 영원할 거라 믿고 외래키를 박는 것은 마이그레이션 비용이 완전히 다르다.

📍 되짚기: `../docs/ep03-admin-implementation/01-storage-and-account.md` / 영상 [03:00]~[03:30]

### 1-I-3

얻는 것: **DB를 조작해도 마스터가 될 수 없다.** 공격면이 다르기 때문이다 — DB에 마스터 플래그가 있으면 DB 쓰기 권한을 얻은 사람은 누구나 스스로를 마스터로 만들 수 있다. 환경변수에 있으면 그러려면 **서버를 재기동할 권한**이 필요하고, 그건 이미 인프라 접근 권한이라 감사 대상이 다르다.

잃는 것: **바꿀 때 서버를 완전히 재기동해야 한다.** 마스터는 자주 바뀌지 않으므로 이 대가는 싸다. 대신 콤마로 여러 명을 지정해 승인자 단일화를 막는다.

📍 되짚기: `../docs/ep03-admin-implementation/01-storage-and-account.md` § 마스터 계정 / 영상 [11:30]~[12:30]

### 1-I-4

틀린 이유: 사내망 안이라는 사실은 **네트워크 위치**를 말할 뿐 그 사람이 이 시스템을 쓸 자격이 있는지는 말하지 않는다. 협력업체 상주 인원·인턴·퇴사 예정자·타 부서가 모두 같은 네트워크에 있다. 그리고 이 시스템이 배분하는 것은 **사내 LLM 토큰과 코드 접근 권한**이다.

3정책: **수동 승인 / 자동 승인 / 필터 승인**(실무 기본).

필터 조건 두 부류: ① **IP 대역** — 확인이 싸지만 위치는 신원이 아니다 ② **사번 인사DB 조회** — 신원에 가깝지만 연동이 필요하다. 보통 둘을 겹친다.

📍 되짚기: `../docs/ep03-admin-implementation/03-signup-policy.md` / 영상 [05:00]~[06:00]

### 1-I-5

**그 사람이 휴가 가면 가입이 멈춘다.** 일반형: **사람도 단일 실패점(SPOF)이다.** 기술 시스템의 SPOF는 이중화로 막는데 승인 절차의 SPOF는 방치되는 일이 흔하다.

해법 둘: ① **마스터를 복수로 둔다**(`.env`에 콤마 — 재기동이 필요하지만 한 번 하면 된다) ② **위임한다**(조직 책임자를 임명해 그 아래를 맡긴다).

📍 되짚기: `../docs/ep03-admin-implementation/03-signup-policy.md` § 승인자 / 영상 [15:30]~[16:30]

### 1-I-6

이 대시보드는 **순수 CRUD**이고 AI가 관여하지 않는다. 그런데도 먼저 만드는 이유는 **권한·모델·감사를 붙일 자리가 여기**여서다 — "이 베이스가 없이는 기업용 에이전트를 만들 수가 없다". 2강 9장의 "정책 서버를 에이전트보다 먼저"가 같은 순서다.

반드시 먼저 넷: **계정의 토큰·세션 시스템 / 세션 감사와 강제 제거 / 인퍼런스 서버(엔드포인트) 등록 / 조직별 모델 라우팅.**

📍 되짚기: `../docs/ep03-admin-implementation/00-overview.md` / 영상 [42:30]~[44:00]

### 1-J-1

**유휴 만료는 보안, 보관 기간은 감사·디스크**다. 유휴는 "이 키를 계속 쓸 수 있나"를 정하고(시연값 7200초), 보관은 "만료된 뒤 기록을 얼마나 들고 있나"를 정한다(86400초).

합치면 잃는 것: 만료 즉시 기록이 사라져 **감사가 불가능**해지거나, 반대로 보관을 위해 만료를 늦추면 **끊겨야 할 세션이 살아 있게** 된다. 두 목적이 반대 방향이라 한 값으로 둘 수 없다.

📍 되짚기: `../docs/ep03-admin-implementation/02-session-and-audit.md` / 영상 [13:00]~[13:30]

### 1-J-2

이유: **사내 보안 방화벽이 L7에서 헤더를 감시**하며 특정 헤더를 넣거나 방해하거나 거부한다. 그래서 이름이 **보안팀과의 협의 대상**이고, 코드에 박으면 고객사마다 빌드를 다시 해야 한다.

둘 다 필요한 이유: **쿠키가 자동 전송되는 것은 브라우저뿐**이다. 안드로이드·아이폰 앱은 획득한 세션 토큰을 **헤더에 계속 주입**해야 세션이 유지된다. 그러니 같은 토큰을 두 경로로 실을 수 있어야 하고, 두 이름 모두 방화벽 통과 승인을 받아야 한다.

📍 되짚기: `../docs/ep03-admin-implementation/02-session-and-audit.md` § 헤더·쿠키 / 영상 [13:30]~[14:30]

### 1-J-3

**서버 로그 적재.** "서버 왜 죽었나 보면 다 로그 쌓여서 죽는 것"이 1위다.

이어지는 설정: **세션 기록 보관 기간**을 처음부터 설정으로 뺀다. 세션 로그는 로그인마다 쌓이고 감사 목적이라 지우기도 조심스럽다. 나중에 붙이려면 이미 쌓인 것을 어떻게 할지부터 정해야 한다.

📍 되짚기: `../docs/ep03-admin-implementation/02-session-and-audit.md` § 로그 적재 / 영상 [13:30]

### 1-K-1

기준: **"양쪽(프론트·서버)이 같은 규격을 봐야 하는가."** 그렇다면 `common`이다.

통신 프로토콜 타입은 **`common/protocol/`**로 간다. 양쪽에 따로 선언하면 **둘이 어긋나는 순간을 컴파일러가 잡지 못한다** — 요청·응답 타입이 다르게 진화해도 빌드가 통과하고 런타임에만 드러난다. 한 곳에 두면 어긋남 자체가 불가능해진다.

📍 되짚기: `../docs/ep03-admin-implementation/04-monorepo-boundaries.md` / 영상 [07:00]~[08:00]

### 1-K-2

이유: **기본값이 거부**가 되기 때문이다. 인증 없이 통과할 경로(로그인·가입)를 예외로 명시하고 나머지 전부를 검사한다. 부수 이점으로 "이 시스템의 권한 규칙"을 한 파일에서 읽을 수 있어 감사에 유리하다.

엔드포인트마다 붙이면 위험한 것: 새 엔드포인트를 추가할 때 검사를 **빼먹으면 조용히 열린다.** 에러도 경고도 없이 그냥 통과하므로 발견되지 않는다.

📍 되짚기: `../docs/ep03-admin-implementation/04-monorepo-boundaries.md` § 퍼미션 / 영상 [08:00]~[08:30]

### 1-K-3

문서에 적은 규칙은 **지시**일 뿐이고, 비결정성이 있는 상대에게 지시만으로는 보장이 되지 않는다. 지켜질 때도 있고 안 지켜질 때도 있다.

함께 줘야 하는 것: **검사 스크립트(정적 린트)**. 그것이 **판정**이다. `스킬(규칙 서술) → 에이전트 작성 → 검사 스크립트(판정) → 위반 시 되돌림` 구조가 되면 규칙이 "적어 둔 바람"이 아니라 통과해야 하는 관문이 된다. (이 학습 레포가 과제마다 테스트를 함께 주는 것과 같은 발상이다.)

📍 되짚기: `../docs/ep03-admin-implementation/07-guardrails-by-structure.md` § 규칙과 검사 / 영상 [28:00]~[28:30]

### 1-K-4

이유: 번역 파일은 **런타임 데이터**다. 키를 문자열로 쓰면 오타를 컴파일러가 잡지 못한다.

오타가 드러나는 시점: **그 언어로 화면을 볼 때**, 빈칸으로. 즉 8개 언어 중 개발자가 보지 않는 언어의 오타는 고객이 발견한다. 타입으로 선언하면 컴파일 시점에 잡힌다. 그래서 뷰에 생 리터럴을 아예 금지한다.

📍 되짚기: `../docs/ep03-admin-implementation/07-guardrails-by-structure.md` § 리소스 키 / 영상 [10:00]~[10:30]

### 1-L-1

**프리셋 = 모델 식별자 + 샘플링 파라미터 조합**이고, 사람이 읽는 이름으로 구분한다.

예: 같은 `gemma4 31b`를 코딩용(temperature 0.4)과 문서 작성용(1.0)으로 동시에 쓰고 싶다. 등록 단위가 "모델"이면 이름이 같아 두 개를 만들 수 없다. 그래서 이름을 달리해 여러 개 등록하고, **이름이 실질적인 키**가 된다.

왜 파라미터를 요청 시점에 보내지 않는가: 클라이언트가 값을 마음대로 바꿔 보낼 수 있으면 **조직별 모델 통제가 사라진다.** 서버가 프리셋 단위로 내려줘야 통제가 유지된다.

📍 되짚기: `../docs/ep03-admin-implementation/05-model-registry.md` § 프리셋 / 영상 [19:00]~[20:00]

### 1-L-2

흔한 실수인 이유: 조직도를 "회사"의 그림으로 생각하면 회사는 하나이므로 루트도 하나가 된다. 하지만 시스템이 담아야 하는 것은 회사가 아니라 **관리 단위의 집합**이다 — 지사·법인·인수한 자회사가 서로의 하위가 아니면서 나란히 존재한다.

비대칭: 루트를 하나로 전제하면 그 가정이 **스키마·쿼리·화면·권한 판정에 다 퍼져** 나중에 고칠 곳이 전부다. 반대로 처음부터 복수를 허용하면 **하나만 쓰는 회사도 아무 문제가 없다.** 한쪽 실수는 비싸고 다른 쪽은 공짜이므로 복수가 기본값이어야 한다.

📍 되짚기: `../docs/ep03-admin-implementation/06-org-tree-and-routing.md` / 영상 [37:00]~[37:30]

### 1-L-3

큰 이유: **조직별 모델 접근 통제.** "이 조직에 속한 애들은 싹 다 이것밖에 못 쓰게 된다 — 내가 지정해 줘야만 쓸 수 있다."

기본값은 **거부**다(지정하지 않으면 못 쓴다). 열어 두고 막는 방식이면 새 모델을 등록할 때마다 전 조직에 열린다. 모델은 곧 비용(VRAM·토큰)이다.

할당 단위는 모델이 아니라 **프리셋**이다 — "gemma를 쓸 수 있다"가 아니라 "gemma-코딩용(0.4) 프리셋을 쓸 수 있다"까지 정해진다.

📍 되짚기: `../docs/ep03-admin-implementation/06-org-tree-and-routing.md` § 모델 통제 / 영상 [38:00]~[38:30]

### 1-L-4

범위 둘: **노드 단독**(그 노드에서만 관리자 — 팀장) / **하위 계층 전체**(그 노드와 모든 후손 — 본부장·지사장).

하위 전체가 없으면: 지사장에게 권한을 주려고 노드마다 개별 부여해야 하고, 그러면 **조직을 새로 만들 때마다 다시 부여**해야 한다. 즉 권한이 조직 변경을 따라오지 못한다. 하위 전체 범위는 "미래에 생길 노드"까지 포함한다.

위임의 제약: 재귀적이되 **자기 권한을 넘지 못한다.** 마스터가 책임자를 임명하고 책임자가 다시 자기 범위 안에서 임명한다 — 팀장이 본부 전체 관리자를 임명할 수는 없다.

📍 되짚기: `../docs/ep03-admin-implementation/06-org-tree-and-routing.md` § 권한 범위 / 영상 [38:30]~[39:30]

### 1-M-1

원인: **Docker 컨테이너 안의 `localhost`는 컨테이너 자신**이다. 관리 서버는 컨테이너 안에 있고 추론 서버(LM Studio)는 호스트에서 도니까 닿지 않는다. 증상이 특징적이다 — 연결은 되는 것 같은데 **요청이 상대에게 도착하지 않는다.**

잘 걸리는 이유: **개발 중에는 잘 됐다.** 컨테이너에 넣기 전 로컬에서 돌릴 때 `localhost`가 맞았기 때문에 설정값이 그대로 남아 **배포 후에만 깨진다.**

📍 되짚기: `../docs/ep03-admin-implementation/05-model-registry.md` § Docker localhost / 영상 [20:30]~[21:00]

### 1-M-2

① **`repeat_penalty`를 준다** (기본은 주지 않는 값이다. 소수값으로)
② **리즈닝 옵션을 끈다** — "리즈닝 쓰면 좋을 것 같지만 루프에 자주 빠져 안 쓰는 경우도 있다"

(1강의 스트림 가드 훅은 같은 증상에 대한 **다른 층**의 처방이다 — 이쪽은 모델 등록 설정, 그쪽은 클라이언트 훅.)

📍 되짚기: `../docs/ep03-admin-implementation/05-model-registry.md` / 영상 [18:30]~[19:00]

### 1-M-3

근거: 단위 테스트의 전통적 가치 중 **"실패 위치를 좁혀 준다"**가 에이전트 앞에서 값이 떨어진다. 좁혀 줘도 에이전트는 그 지점을 **다시 생성(뽑기)**할 뿐이고, 통과한 결과가 "옳아서 통과"인지 "그 테스트만 만족"인지 구별되지 않는다. 그렇다면 같은 뽑기로 **사용자가 실제 겪는 경로**를 통과시키는 편이 낫다.

성립하지 않는 영역: **순수 로직이 두꺼운 곳.** 강의가 만든 것은 CRUD 관리 화면이라 인수 경로가 곧 기능의 전부다. 반면 이 레포의 과제들(컴팩션 트리거 판정, 조직 트리 권한 해석, 승인 필터)은 입력·출력이 명확한 함수이고, 여기서 단위 테스트는 실패 위치를 좁히려고 쓰는 게 아니라 **무엇을 만들지 정의하려고** 쓴다 — 즉 **명세 그 자체**다.

정리: 교훈은 "버려라"가 아니라 **"테스트의 층위를 목적으로 고르라"**다.

📍 되짚기: `../docs/ep03-admin-implementation/08-acceptance-first-testing.md` / 영상 [41:00]~[41:30]

---

## 파트 2. 판단 문제

### 2-1

먼저 짚어야 할 것: **이 질문에는 하드웨어 제약이 숨어 있다.** 1-C-6의 산수로 모델 적재 후 남는 VRAM은 약 200GB이고 풀 컨텍스트 세션이 10GB를 넘으므로 **동시 세션은 약 20개**다. 개발자 40명이 동시에 붙을 수 없다 — 즉 **세션 서버 방식보다 먼저 정해야 할 것이 세션 배분 정책**이다.

그 위에서:
- **스트림 서버가 유리한 조건**: 원격·모바일 접근 요구, 여러 클라이언트가 같은 세션을 공유해야 함, 세션을 대기열로 관리해야 함(점유 개념이 없으면 대기·인계가 쉽다)
- **세션별 인스턴스가 유리한 조건**: CLI 전용 납품(세션 하나만 감당), 클라이언트가 데스크톱 IDE 하나로 고정, 이벤트 큐 인프라를 온프레미스에 추가 설치하기 어려움

흔한 오답: "스트림이 최신이라 무조건 스트림." 온프레미스에서 미들웨어 하나 추가는 곧 고객사 환경에서 깨질 지점 하나 추가다(2-6과 같은 판단).

📍 되짚기: `../docs/ep02-business-agent/02-session-server.md` + `../docs/ep01-concepts/06-local-llm.md`

### 2-2

- **모델 요약이 나은 조건**: 기간이 짧다. 구현이 프롬프트 한 번으로 끝난다. 상세 조회 도구를 만들 여력이 없다. 세션 길이가 짧아 컴팩션이 드물게 일어난다.
- **검색형 요약이 나은 조건**: 세션이 길고 컴팩션이 반복된다(열화가 세대별로 누적). 도구 인프라가 이미 있다. 민감한 코딩 작업이라 정보 분실 비용이 크다.

결정을 가르는 변수는 **"컴팩션이 한 세션에서 몇 번 일어나는가"** 다. 1~2회면 모델 요약의 열화를 감수할 수 있고, 5회 이상 반복되면 열화가 누적돼 세션 후반이 못 쓰게 된다.

기간이 짧을 때의 중간 선택: 모델 요약 + **컴팩트 레코드 병합**(이전 요약들을 함께 입력)으로 열화를 완화한다. 이게 RAG 청크 병합에서 온 기법이다.

📍 되짚기: `../docs/ep02-business-agent/04-compaction.md` § 문제 2

### 2-3

CLI형은 **세션 하나만 감당**한다는 점이 앞의 세 형태와 결정적으로 다르다. 다중 세션은 창을 여러 개 띄우는 방식(tmux)으로만 가능하다.

다시 결정해야 하는 것:
1. **세션 모델** — "한 클라이언트가 여러 세션을 관리한다"는 전제로 만든 상태 관리·화면 전환 로직이 무의미해진다
2. **세션 배분 정책** — 사용자당 세션 1개가 기본이 되므로 동시 세션 견적이 달라진다(하드웨어 산수도 달라진다)
3. **웹 UI의 처분** — 버릴 것인가, 관리자 대시보드(비즈니스 서버)로 전용할 것인가. 후자면 손실이 작다

이 문제의 교훈이 "배포 결정이 코어로 역류한다"다. 고객 환경(터미널 전용)을 먼저 확인하지 않으면 절반을 버린다.

📍 되짚기: `../docs/ep02-business-agent/08-deployment-modes.md` § 축 2

### 2-4

충돌: 불변 리스트(이벤트 소싱)의 전제는 **앞이 다 이어져서 현재가 만들어진다**는 것이다. 그런데 컴팩트는 앞부분을 버리거나 압축한다 — 즉 "앞이 다 이어진다"는 전제를 스스로 깨뜨린다.

해소: 리스트에서 실제로 삭제하지 않는다. **컴팩트 레코드를 스냅샷 구분점으로 삼아, 모델에 보낼 때만 "마지막 컴팩트 아래"를 선택**한다. 로그 자체는 append-only로 남는다. 즉 **저장 구조(불변)와 재생 범위(가변)를 분리**해 충돌을 없앤다.

이어지는 문제: 컴팩트가 반복되면 세대별 열화가 쌓인다 → **컴팩트 레코드 병합**(RAG 청크 병합 기법)으로 완화한다.

📍 되짚기: `../docs/ep02-business-agent/04-compaction.md` § 컴팩트 레코드와 불변 리스트

### 2-5

문제: 구현은 이미 에이전트가 빠르게 한다. 병목은 **무엇을 시킬지**이고, 그것은 혼자 세우면 품질이 떨어진다. "넌 너대로, 난 나대로"는 팀이 아니라 개인 작업의 병렬이며, 각자 헤매면서 **토큰만 낭비**하는 패턴이 된다.

대안: 낮에 팀이 모여 계획 문서를 확정하고 → 밤에 에이전트가 실행하고 → 아침에 팀이 리뷰한다. 계획 문서에 반드시 들어갈 항목: 목적 / 규칙 / 마일스톤 공통 사이클(구현 전 기준 확인 → 검증 → 커밋·푸시 → 배포) / 추론 게이트 / 로컬 테스트 규정 / **완료 판정 기준** / 공통 추론 입력 / **증거 저장 방법** / 단계별 지시와 **완료 시 갱신할 위치**.

마지막 셋(완료 판정·증거 저장·갱신 위치)이 빠지면 아침 리뷰의 대상이 남지 않는다.

📍 되짚기: `../docs/ep02-business-agent/05-team-operating-model.md`

### 2-6

- **하나로 합치는 쪽(강의의 선택)**: 온프레미스에서 **미들웨어 수 = 고객사 환경에서 깨질 지점 수**다. 설치·운영·보안 심사 대상이 줄어든다. 그리고 스루풋이 부족하지 않다 — **모델 추론이 이미 병목**이라 카프카급 처리량이 필요 없다.
- **나누는 쪽이 나은 조건**: 에이전트 외 다른 시스템도 같은 이벤트 스트림을 소비해야 함, 이벤트 보존 기간·재처리 요구가 커서 전용 로그 스토리지가 필요함, 고객사에 이미 카프카 운영 조직이 있음.

결정을 가르는 변수는 **"이 스트림을 에이전트 말고 누가 더 소비하는가"** 다. 소비자가 하나면 합치는 쪽이 유리하다.

📍 되짚기: `../docs/ep02-business-agent/10-tech-stack.md` § 베이스

### 2-7

**전용 도구 다수가 유리하다.** 200B Q5는 모델 크기·양자화 하한에 겨우 걸친 구성이다. 이 강도에서는 쉘 명령을 조합해 정확히 만들어내는 데서 실수가 난다. 전용 도구는 인자 스키마가 좁아 실수 여지를 구조적으로 줄인다.

**범용 쉘이 나은 조건**: 프런티어급 강한 모델을 쓴다(Codex가 쉘 통합을 택한 배경), 도구를 만들 인력이 없다, 작업 종류가 예측 불가해 전용 도구를 미리 설계할 수 없다.

결정 변수는 **모델 강도**다. Claude Code(전용 다수)와 Codex(쉘 통합)의 차이가 이 선택의 두 사례다.

주의: 전용 도구를 늘리면 도구 정의가 기반 컨텍스트를 잠식한다 — 5% 예산(1-F-2)과 함께 봐야 한다.

📍 되짚기: `../docs/ep01-concepts/03-tools-as-control.md` § 도구 설계의 세 축

### 2-8

**강한 모델일수록 훅을 뺀다.** 훅은 모델이 스스로 못 하는 것을 코드가 대신 보장하는 장치다. 강한 모델은 그 판단을 스스로 하므로, 훅이 많으면:

1. 모델의 올바른 판단을 코드가 덮어써 오히려 품질이 떨어진다
2. 훅이 주입하는 텍스트가 컨텍스트를 잠식하고 희석에 기여한다
3. 모델이 바뀔 때마다 훅을 재조정해야 하는 유지 비용이 생긴다

대안: 훅은 **결정적으로 보장해야 하는 것**에만 쓴다 — 포맷팅, 금지 경로 차단, 시크릿 유출 방지처럼 판단이 필요 없는 규칙. 판단이 필요한 검증은 모델에게 맡기고, 실패했을 때 관찰 결과로 알려주는 편이 낫다(과제 3-1의 에러 경로와 같은 원리).

📍 되짚기: `../docs/ep01-concepts/04-hooks.md` § 강한 모델일수록 훅을 뺀다

### 2-9

**반대할 근거가 있다.** "사내망이라 안전하다"는 **네트워크 위치**를 말할 뿐이고 신원 증명이 아니다. 협력업체 상주 인원·인턴·타 부서·퇴사 예정자가 같은 네트워크에 있다. 그리고 이 시스템이 배분하는 것은 **사내 LLM 토큰과 코드 접근 권한**이므로 가입은 곧 자원 배분이다.

받아들일 조건: 사내망 접근 자체가 이미 강하게 통제되고(예: NAC + 사번 인증 후에만 네트워크 진입), 그 통제의 감사 기록을 우리가 신뢰할 수 있을 때. 이때는 인증이 앞단에서 이미 끝난 것이므로 자동 승인이 중복 절차가 된다.

대안 설계: **필터 승인**으로 간다. `허용 IP 대역 + 사번 인사DB 조회`를 AND로 걸고, 필수 가입 JSON에 부서·사번을 받는다. 통과 못한 신청은 거부가 아니라 **보류**로 떨어뜨려 관리자가 보게 한다. 그리고 승인자를 복수로 둔다(휴가에 멈추지 않게).

덧붙일 것: 필터를 나중에 강화할 수 있게 **처음부터 필드를 넉넉히 받아 둔다** — 나중에 받기 시작한 필드는 기존 계정에 없어 소급 적용이 안 된다.

📍 되짚기: `../docs/ep03-admin-implementation/03-signup-policy.md`

### 2-10

무너지는 것: **조직별 모델 통제.** 관리자가 온도 0.4로 묶어 둔 프리셋에 클라이언트가 1.9를 실어 보내면 그만이다. 조직에 모델을 할당한 의미가 "어느 모델을 부를 수 있나"까지로 줄어들고, **어떻게 부르는가**는 통제 밖으로 나간다. 산업용에서 이 구분이 중요한 이유는 파라미터가 품질뿐 아니라 **비용(컨텍스트 크기)**과 안전성(temperature)에 직결되기 때문이다.

화면이 단순해진다는 이점은 실재한다. 하지만 그 단순함은 관리 화면에서 클라이언트로 **복잡도를 옮긴 것**이고, 옮겨진 쪽은 통제할 수 없는 곳이다.

정상 경로: 다른 파라미터가 필요하면 **관리자가 프리셋을 하나 더 만든다.** `gemma-coding`(0.4)과 `gemma-writing`(1.0)을 따로 등록하고 조직에 필요한 것만 할당한다. 사용자는 파라미터를 고르는 게 아니라 **프리셋을 고른다** — 선택의 자유는 유지되고 경계는 관리자가 정한다.

📍 되짚기: `../docs/ep03-admin-implementation/05-model-registry.md` § 프리셋

### 2-11

성격이 다른 두 시간 설정: **세션 유휴 제한 시간(7200초)**과 **보관 기간(86400초)**.

- 유휴 제한 → **보안**. "이 키를 계속 쓸 수 있나"를 정한다. 짧으면 안전하고 불편하다
- 보관 기간 → **감사·디스크**. "만료된 기록을 얼마나 들고 있나"를 정한다. 길면 추적이 쉽고 디스크를 먹는다

하나로 합치면 잃는 것: 두 목적이 **반대 방향**이라 한 값이 둘을 만족할 수 없다. 보안을 위해 짧게 잡으면 감사 기록이 금방 사라지고, 감사를 위해 길게 잡으면 끊겨야 할 세션이 살아 있게 된다. 실제로 서버가 죽는 원인 1위가 로그 적재라서 보관 쪽은 반드시 상한이 필요하고, 그 상한을 유휴 시간과 같게 둘 이유는 없다.

(같은 화면의 "만료 세션 기록: 만료 기록 유지"가 세 번째 축이다 — 보관을 아예 하지 않는 선택.)

📍 되짚기: `../docs/ep03-admin-implementation/02-session-and-audit.md`

### 2-12

반대 근거: 시스템이 담는 것은 "회사"가 아니라 **관리 단위의 집합**이다. 지사·해외 법인·인수한 자회사·독립 프로젝트 조직은 서로의 하위가 아니면서 나란히 존재한다. 그리고 **비대칭**이 결정적이다 — 복수를 허용해도 루트가 하나인 회사는 아무 불편이 없지만, 하나로 제한한 뒤 둘이 되면 전면 수정이다.

루트 하나로 갔을 때 고쳐야 할 곳:

- **스키마** — 루트를 별도 테이블·플래그·`parentId NOT NULL`로 표현했다면 전부
- **조회 쿼리** — "루트 하나를 가져와 트리를 만든다"는 전제의 모든 쿼리
- **트리 UI** — 최상위가 하나라고 가정한 렌더링·드래그 이동·"형제 추가" 버튼의 유무
- **권한 판정** — 조상 사슬을 거슬러 올라가다 루트에서 멈추는 로직, 그리고 "하위 전체" 범위가 회사 전체를 의미한다고 가정한 곳
- **모델 할당 상속** — 루트 할당이 전사 기본값이라는 전제
- **마이그레이션** — 기존 루트를 새 루트 아래로 옮기지 않고 나란히 놓아야 하므로 데이터 이동이 아니라 구조 변경이다

`parentId: string | null`로 시작하면 이 전부가 처음부터 성립한다.

📍 되짚기: `../docs/ep03-admin-implementation/06-org-tree-and-routing.md`

---

## 파트 3. 재현 과제

코딩 과제 **3-1~3-5의 정답은 이 파일에 없다.** 판정은 `tests/{번호}-{slug}.test.ts`가 하고, 참고 구현은 `solutions/{번호}-{slug}.ts`에 있다.

```bash
cd packages/coding-agent-architecture
pnpm test e01-02-01              # 내 구현으로 판정
```

실패 항목에는 `↳ 힌트`가 붙어 있다. 막히면 `solutions/`를 먼저 열지 말고 **힌트를 읽고 해당 문서로 돌아간다** — 참고 구현은 통과한 뒤 접근을 비교하는 용도다.

### 3-10 — 판정 체크리스트 (코딩 아님)

배포 형태 결정표는 정답이 하나가 아니다. 아래를 만족하면 통과로 본다.

- [ ] **금융권**: 클라이언트가 **CLI형**이어야 한다(터미널 전용 환경). 그 결과 "세션 하나만 감당"이 사양에 반영됐는가
- [ ] **제조사**: 온프레미스 + 특수 환경(임베디드·모션 제어기 등) 가능성을 언급했는가. 라이센스 기간과 **시간 조작 방어**가 과금 항목에 있는가
- [ ] **웹 서비스 회사**: 도커가 실제로 유리한 유일한 케이스다(리눅스 기반 이미지 세트). 여기서 도커를 골랐다면 근거가 "리눅스만 맞추면 된다"인가
- [ ] 최소 한 곳에서 **일반론과 반대되는 선택**이 나왔는가 — 예: 보안이 가장 중요한 금융권인데 RHEL 컨테이너 차단 때문에 도커를 못 쓰고 네이티브를 고른 경우
- [ ] 각 결정에 **고객 환경의 사실**이 근거로 붙었는가. "보안이 중요하므로 도커"처럼 일반론만 적었다면 다시 쓴다

흔한 오답: 세 고객 모두에게 같은 조합(도커 + 웹 UI + 구독 과금)을 제시하는 것. 세 축이 고객 환경에 따라 갈리지 않으면 이 장을 읽은 의미가 없다.

📍 되짚기: `../docs/ep02-business-agent/08-deployment-modes.md`
